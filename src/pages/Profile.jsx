const Profile = () => {
  return (
    <section className="page">
      <h1 className="page-title">Profile</h1>
      <p className="page-text">
        Profile page placeholder. React Router keeps the navbar visible and
        only swaps the content below it, which is typical for SPA navigation.
      </p>
    </section>
  );
};

export default Profile;
