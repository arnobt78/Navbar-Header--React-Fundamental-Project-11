const About = () => {
  return (
    <section className="page">
      <h1 className="page-title">About</h1>
      <p className="page-text">
        This demo uses React Router so that navbar links switch the visible
        content without a 404. The URL updates and the correct component is
        rendered — a simple example of single-page application (SPA) routing.
      </p>
    </section>
  );
};

export default About;
